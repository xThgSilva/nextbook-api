package com.nextbook.responses;

import com.nextbook.entities.User;

public class UserAllUsersResponseDTO extends UserBaseResponseDTO{
	public UserAllUsersResponseDTO(User user) {
		super(user);
	}
}
